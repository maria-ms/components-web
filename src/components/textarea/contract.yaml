html:
  element: textarea
aria:
  aria-invalid: validation feedback
figma:
  preview: [State, Content, Displayed text]
rules:
  - Preserve native textarea behaviour, including browser resize, until a public decision changes it.
  - Field or the form decides when invalid feedback appears.
