**Plan**

Run these prompts in order. Do not let the Figma Agent edit everything in one pass.

The overall goal is to clean up component sizing variables so the code package can consume generated tokens instead of hardcoded dimensions.

Right now Figma is correctly using variables for colors, typography, spacing, radius, borders, and shadows. However, several component widths, heights, and icon sizes still appear to exist only as raw Figma layer dimensions. Raw dimensions do not export into the token package, so engineers cannot reference them as generated CSS variables.

The goal is not to duplicate values. The goal is to expose component intent in the exported variables.

Component variables should alias existing primitive or semantic variables where possible. For example, if 16px is `space/05`, then `component/input/icon/size` should alias `space/05`.

Creating variables is not enough. The variables must be bound to the actual component geometry where Figma supports binding: icon frame sizes, avatar sizes, control heights, dropdown item heights, component widths, and variant dimensions.

If Figma cannot bind a variable to a specific dimension, report that limitation instead of silently leaving the raw value in place.

**Geometry ownership policy**

Components own intrinsic geometry. Consumers own placement geometry.

Component-owned geometry includes control height, padding, internal gap, icon size, avatar size, OTP cell size, dropdown item min-height, focus ring, border radius, and other dimensions required for the component to look correct in isolation.

Consumer-owned geometry includes width, max-width, grid/flex placement, columns, wrapping, and whether controls are arranged vertically or horizontally.

Only add width variables when the width represents a reusable component contract or canonical composition default. Do not turn page/artboard/example widths into forced component defaults.

**Prompt 1 - Audit Only**

```txt
Overall goal: clean up component sizing variables so exported Figma variables can generate code tokens for component geometry. Do not redesign components. Do not change visual values unless a value is clearly wrong. Do not edit anything in this pass.

Audit these components:
- button
- avatar
- text input
- search input
- number input
- select input
- OTP input
- dropdown
- alert
- accordion
- field
- field group

Find and report:
1. Which icon sizes are currently raw dimensions.
2. Which component widths are currently raw dimensions.
3. Which component heights or min-heights are currently raw dimensions.
4. Which avatar sizes, status sizes, and badge sizes are currently raw dimensions.
5. Which values are already bound to variables.
6. Which values should alias existing primitive or semantic variables.
7. Which values need new component-level variables.
8. Which dimensions cannot be variable-bound in Figma, if any.

Use this naming shape for proposed variables:
- `component/<component>/<part>/<property>`
- `component/<component>/<part>/<property>/<size>`
- `component/<component>/<property>/<variant>`

Examples:
- `component/input/icon/size`
- `component/select/option/icon/size`
- `component/dropdown/item/icon/size`
- `component/avatar/status/size/md`
- `component/alert/width/large`

Stop after the report. Do not make changes.
```

**Prompt 2 - Icon Size Variables**

```txt
Overall goal: clean up component sizing variables so exported Figma variables can generate code tokens for component geometry. Do not redesign components. Preserve the current visual values.

Add and bind component-level icon size variables.

Use this naming shape:
- `component/input/icon/size` = 16px
- `component/select/icon/size` = 16px
- `component/select/option/icon/size` = 16px
- `component/dropdown/item/icon/size` = 16px
- `component/dropdown/trigger/icon/size` = 24px
- `component/button/icon/size/sm`
- `component/button/icon/size/md`
- `component/button/icon/size/lg`
- `component/alert/icon/size`
- `component/accordion/icon/size`

Alias these to existing primitive or semantic variables where possible. For example, if 16px is `space/05`, then `component/input/icon/size` should alias `space/05`.

Bind the variables to the actual icon frames/layers where Figma supports variable binding.

After changes, report:
1. Variables created.
2. Existing variables reused as aliases.
3. Components/layers where each variable was bound.
4. Any icon dimensions that could not be bound to variables.
5. Any ambiguity that needs designer review.
```

**Prompt 3 - Form Control Geometry Variables**

```txt
Overall goal: clean up component sizing variables so exported Figma variables can generate code tokens for component geometry. Do not redesign components. Preserve the current visual values.

Add and bind component-level variables for form control geometry.

Input:
- `component/input/height`
- `component/input/width/default` = 276px
- `component/input/width/small` = 140px
- `component/input/inline-width/default` = 329px
- `component/input/inline-width/small`, if applicable
- `component/input/prefix-suffix/icon/size` = 16px

Input OTP:
- `component/input-otp/cell/size`

Select:
- `component/select/width/default` = 276px
- `component/select/icon/size` = 16px
- `component/select/option/icon/size` = 16px

Field and Field Group:
- Add variables only for intrinsic field spacing or grouped-control geometry.
- Do not add fixed page/layout widths unless the width is part of the reusable component contract.
- If grouped controls need a stable attached-control width, propose a variable and report it before applying.

Alias these to existing primitive or semantic variables where possible.

Bind the variables to actual control heights, widths, icon frames, OTP cells, and select option icon dimensions where Figma supports variable binding.

After changes, report:
1. Variables created.
2. Existing variables reused as aliases.
3. Components/layers where each variable was bound.
4. Any dimensions that could not be bound to variables.
5. Any ambiguity that needs designer review.
```

**Prompt 4 - Avatar, Dropdown, Accordion, Alert, And Button Geometry Variables**

```txt
Overall goal: clean up component sizing variables so exported Figma variables can generate code tokens for component geometry. Do not redesign components. Preserve the current visual values.

Add and bind component-level variables for the remaining component geometry.

Avatar:
- `component/avatar/size/xs`
- `component/avatar/size/sm`
- `component/avatar/size/md`
- `component/avatar/size/lg`
- `component/avatar/size/xl`
- `component/avatar/size/2xl`
- `component/avatar/status/size/xs`
- `component/avatar/status/size/sm`
- `component/avatar/status/size/md`
- `component/avatar/status/size/lg`
- `component/avatar/status/size/xl`
- `component/avatar/status/size/2xl`
- `component/avatar/company-badge/size/xs`
- `component/avatar/company-badge/size/sm`
- `component/avatar/company-badge/size/md`
- `component/avatar/company-badge/size/lg`
- `component/avatar/company-badge/size/xl`
- `component/avatar/company-badge/size/2xl`

Dropdown:
- `component/dropdown/menu/width` = 240px
- `component/dropdown/item/min-height` = 36px
- `component/dropdown/item/icon/size` = 16px
- `component/dropdown/trigger/icon/size` = 24px
- `component/dropdown/trigger/avatar/size` = 40px

Accordion:
- `component/accordion/width/default` = 353px
- `component/accordion/width/card` = 440px
- `component/accordion/icon/size`

Alert:
- `component/alert/width/large` = 976px
- `component/alert/width/medium` = 560px
- `component/alert/width/small` = 343px
- `component/alert/icon/size`

Button:
- `component/button/height/sm`, if there is a small button height
- `component/button/height/md`, if there is a default/medium button height
- `component/button/height/lg`, if there is a large button height
- `component/button/icon/size/sm`
- `component/button/icon/size/md`
- `component/button/icon/size/lg`

Alias these to existing primitive or semantic variables where possible.

Bind the variables to actual avatar frames, status indicators, badge frames, dropdown menu/item dimensions, accordion widths/icons, alert widths/icons, and button dimensions where Figma supports variable binding.

After changes, report:
1. Variables created.
2. Existing variables reused as aliases.
3. Components/layers where each variable was bound.
4. Any dimensions that could not be bound to variables.
5. Any ambiguity that needs designer review.
```

**Prompt 5 - Final Audit**

```txt
Overall goal: verify that component sizing variables can now be exported and consumed by code. Do not edit anything in this pass.

Run a final audit on:
- button
- avatar
- text input
- search input
- number input
- select input
- OTP input
- dropdown
- alert
- accordion
- field
- field group

Check:
1. Icon sizes use component-level variables where appropriate.
2. Component-owned widths use component-level variables where appropriate.
3. Component-owned heights and min-heights use component-level variables where appropriate.
4. Avatar sizes, status sizes, and badge sizes use component-level variables.
5. Component variables alias primitive or semantic variables where possible.
6. No raw values remain where a variable exists and can be bound.
7. No page/artboard-only example widths were turned into component defaults by mistake.
8. No visual values were changed unintentionally.
9. The variables will export through Figma Export Modes.

Report:
- pass/fail for each check
- remaining raw dimensions
- exact components/layers that still need manual cleanup
- variables that were created but not bound
- dimensions that Figma cannot bind to variables
- anything that needs engineer review after the next token export
```

Run Prompt 1 first and review the report before allowing edits. Then run the edit prompts one by one. After Prompt 5 passes, export modes again and send the updated JSON files to engineering.
